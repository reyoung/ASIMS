require(["admin/active"],function(a){$(function(){a("nav_airlinePlan_list");$(".btn-group .btn.btn-danger").click(function(){console.debug("On Delete Btn Click");idToDelete=$(this).attr("data-id");console.debug("The Data need to delete is "+idToDelete)})})});
